#!/bin/bash

# A shortcut for tmuxinator
alias mux=tmuxinator

# A shortcut for docker-compose
alias compose=docker-compose

# A shortcut for ccat
alias cat=ccat

# Source: http://homeonrails.com/2016/05/truecolor-in-gnome-terminal-tmux-and-neovim/
# See: ~/.terminfo/xterm-256color-italic.terminfo
# alias tmux="env TERM=xterm-256color tmux"

# A shortcut for kubernetes kubectl
alias kube=kubectl

# Show mouse pointer again
# See: https://ask.fedoraproject.org/en/question/31186/my-mouse-cursor-dissapears-when-using-gnome3/?answer=49702#post-id-49702
alias mouse='dconf write /org/gnome/settings-daemon/plugins/cursor/active false'

# Easier navigation: .., ..., ...., ....., ~ and -
alias ..="cd .."
alias ...="cd ../.."
alias ....="cd ../../.."
alias .....="cd ../../../.."
alias ~="cd ~"
alias -- -="cd -"

colorflag="--color"

# List all files colorized in long format
alias ll="ls -lah ${colorflag}"

# List all files colorized in long format, including dot files
alias la="ls -laF ${colorflag}"

# List only directories
alias lsd="ls -lF ${colorflag} | grep --color=never '^d'"

# Always use color output for `ls`
alias ls="command ls ${colorflag}"

# Always enable colored `grep` output
alias grep='grep --color=auto '

# Enable aliases to be sudo’ed
alias sudo='sudo '

# Copy file interactive
alias cp='cp -i'

# Move file interactive
alias mv='mv -i'

# Untar
alias untar='tar xvf'

# Tar
alias targz='tar -czvf'

# Emit message when task is done
# Usage: docker-compose build --no-cache && compose up && rdy
alias rdy='notify-send "Task is done"'

# quickly get into fuelup folder
alias fu='cd ~/Projects/fuelup'

# quickly get into Projects folder
alias pro='cd ~/Projects'

# Yeah I know, don't judge me!
alias gti='git'

# What the hell did I do yesterday
alias standup='cd ~/Projects/fuelup && git log --pretty=oneline --abbrev-commit --decorate --author="Jan Pieter" --author="erroneousboat" --since=$(date --date="1 day ago" +"%Y-%m-%d")'

# Useful extraction function:
# Source: https://github.com/xvoland/Extract
function extract {
 if [ -z "$1" ]; then
    # display usage if no parameters given
    echo "Usage: extract <path/file_name>.<zip|rar|bz2|gz|tar|tbz2|tgz|Z|7z|xz|ex|tar.bz2|tar.gz|tar.xz>"
 else
    if [ -f "$1" ] ; then
        NAME=${1%.*}
        #mkdir $NAME && cd $NAME
        case "$1" in
          *.tar.bz2)   tar xvjf ./"$1"    ;;
          *.tar.gz)    tar xvzf ./"$1"    ;;
          *.tar.xz)    tar xvJf ./"$1"    ;;
          *.lzma)      unlzma ./"$1"      ;;
          *.bz2)       bunzip2 ./"$1"     ;;
          *.rar)       unrar x -ad ./"$1" ;;
          *.gz)        gunzip ./"$1"      ;;
          *.tar)       tar xvf ./"$1"     ;;
          *.tbz2)      tar xvjf ./"$1"    ;;
          *.tgz)       tar xvzf ./"$1"    ;;
          *.zip)       unzip ./"$1"       ;;
          *.Z)         uncompress ./"$1"  ;;
          *.7z)        7z x ./"$1"        ;;
          *.xz)        unxz ./"$1"        ;;
          *.exe)       cabextract ./"$1"  ;;
          *)           echo "extract: '$1' - unknown archive method" ;;
        esac
    else
        echo "'$1' - file does not exist"
    fi
fi
}
